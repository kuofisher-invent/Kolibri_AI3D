[package]
name = "kolibri-piping"
version = "0.1.0"
edition = "2021"

[dependencies]
kolibri-core = { path = "../core" }
glam = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
